import React from "react";
import ReactDOM from "react-dom";

import Locofast from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Locofast />
  </React.StrictMode>,
  rootElement
);
